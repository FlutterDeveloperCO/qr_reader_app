import 'package:flutter/material.dart';

class HistoryUrlsScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('History URLs'),
    );
  }
}